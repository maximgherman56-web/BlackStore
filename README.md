# My Marketplace

Ласкаво просимо до **My Marketplace**! Це веб-застосунок на Django, який дозволяє користувачам купувати та продавати товари онлайн.

## 🚀 Функціонал

*   **Користувачі**:
    *   Реєстрація та авторизація.
    *   Можливість бути покупцем або продавцем.
*   **Товари**:
    *   Перегляд товарів за категоріями.
    *   Детальний опис товару з фото.
    *   Додавання товарів (для продавців).
*   **Кошик**:
    *   Додавання та видалення товарів.
    *   Перегляд вмісту кошика.
*   **Замовлення**:
    *   Оформлення замовлення з вказанням даних для доставки.
    *   Історія замовлень (в розробці).

## 🛠 Технології

*   **Python 3.x**
*   **Django**
*   **SQLite** (стандартна база даних)
*   **HTML/CSS** (Bootstrap або кастомні стилі)

## 📦 Встановлення та запуск

Слідуйте цим крокам, щоб запустити проект локально:

1.  **Клонуйте репозиторій** (якщо ви використовуєте git):
    ```bash
    git clone <посилання-на-ваше-репо>
    cd my_marketplace
    ```
    *Або просто відкрийте папку з проектом.*

2.  **Створіть віртуальне середовище** (рекомендовано):
    ```bash
    python -m venv venv
    # Windows
    venv\Scripts\activate
    # macOS/Linux
    source venv/bin/activate
    ```

3.  **Встановіть залежності**:
    В проекті поки немає `requirements.txt`, тому встановіть основні пакети вручну:
    ```bash
    pip install django pillow
    ```

4.  **Застосуйте міграції**:
    ```bash
    python manage.py migrate
    ```

5.  **Створіть суперкористувача (адміна)**:
    ```bash
    python manage.py createsuperuser
    ```

6.  **Запустіть сервер**:
    ```bash
    python manage.py runserver
    ```

7.  **Відкрийте в браузері**:
    Перейдіть за посиланням [http://127.0.0.1:8000/](http://127.0.0.1:8000/)

## 📂 Структура проекту

*   `manage.py`: Утиліта командного рядка Django.
*   `marketplace/`: Налаштування проекту (settings, urls).
*   `store/`: Основний додаток магазину (models, views, templates).
*   `media/`: Завантажені користувачами медіа-файли (зображення товарів).
*   `db.sqlite3`: База даних.

## 📝 Автор

Створено Maxim.
